import React from "react";

const SearchBox = () => {
  return (
    <div>
      <input
        className="pa3 ba b--green bg-lightest-blue"
        type="text"
        placeholder="Search..."
      />
    </div>
  );
};

export default SearchBox;
